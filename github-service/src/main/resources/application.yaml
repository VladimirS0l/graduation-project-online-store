server:
  port: 8888

spring:
  application:
    name: github-config

  config:
    import: optional:file:.env[.properties]
#    additional-location: .env

  cloud:
    config:
      server:
        git:
          uri: https://github.com/VladimirS0l/config_eureka.git
          clone-on-start: true
          basedir: file://c:/tmp/git-c
          username: VladimirS0l
          password: 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
          default-label: main

#          uri: ${CONFIG_SERVER_URI}
#          clone-on-start: true
#          basedir: ${CONFIG_SERVER_FILE}
#          username: ${CONFIG_SERVER_USERNAME}
#          password: ${CONFIG_SERVER_PASSWORD}
#          default-label: main
